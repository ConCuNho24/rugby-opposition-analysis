# Architecture-test input only

`reformatted_public_sample.csv` is a tiny deliberately reformatted version of
public event annotation values. It is **not** another real provider and is never
mixed into real-data profiling, processing, metrics, charts or reports.

Its sole purpose is to show that a second adapter can emit the same canonical
event model and reuse the same downstream analysis code.
